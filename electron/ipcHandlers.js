'use strict';

/**
 * Registro central de handlers IPC (ampliar cuando existan módulos).
 * Por ahora el proceso principal registra solo IPC básico en main.js.
 */

function registerIpcHandlers() {}

module.exports = { registerIpcHandlers };
